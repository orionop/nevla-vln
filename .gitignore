.vscode

.DS_Store

# local Claude settings (personal, not shared)
.claude/settings.local.json

# secrets / env — ignore all .env files EXCEPT the committed template
.env
.env.*
!.env.example

# SysNav reference clone (vendored selectively later, not committed wholesale)
_reference_SysNav/

# Python
__pycache__/
*.pyc
.pytest_cache/

# eval harness runtime artifacts
eval_harness/runs/
debug/
output/

# VLA-3D dataset + derived data (third-party license; regenerate from VLA3D_DIR).
# Keep the loader/tools in git, not the data itself.
vla3d/
*.zip
eval_harness/objref_candidates.json
eval_harness/viz/
